# FinBERT-Stock-Forecaster

An AI-powered financial analysis tool that combines **Natural Language Processing (NLP)** and **Deep Learning (LSTM)** to predict stock trends.

Unlike traditional predictors that only look at price history, this project analyzes the **news sentiment** driving the market using Google's BERT architecture (FinBERT).
